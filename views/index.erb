<table>
<tr><th>Contact</th><th>Address</th><th>Location</th></tr>
<% @locations.each do |contact| %>
   <tr>
	<td><%= contact.name %></td>
	<td><%= contact.address %></td>
	<td><%= contact.lat %></td>
	<td><%= contact.lng %></td>
   </tr>
<% end %>
</table>